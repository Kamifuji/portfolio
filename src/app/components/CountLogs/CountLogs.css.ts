import { style } from '@vanilla-extract/css'

export const styles = {
  section: style({
    display: 'grid',
    
  }),
  button: style({
    fontSize: 24,
  }),
  list: style({
    display: 'grid',
  })
}
