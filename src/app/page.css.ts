import { style } from "@vanilla-extract/css";

export const styles = {
  main: style({
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    gap: 100,
    padding: 90,
    background: 'white'
  })
}